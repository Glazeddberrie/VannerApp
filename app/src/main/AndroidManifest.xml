<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:enableOnBackInvokedCallback="true"
        android:icon="@drawable/gtalogo3_1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/gtalogo3_1"
        android:supportsRtl="true"
        android:theme="@style/Theme.VannerApp"
        tools:targetApi="31">
        <activity
            android:name=".UI.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.UserDash.Home" />
        <activity android:name=".UI.UserDash.Perfil" />
        <activity android:name=".UI.UserDash.Offers" />
        <activity android:name=".UI.UserDash.ModPerfil" />
        <activity android:name=".UI.Register" />

    </application>

</manifest>